{
  "name": "homeslate-hosted",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --pretty false",
    "test": "vitest run"
  }
}
